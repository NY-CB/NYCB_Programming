
 def profile(name, age, main):
    print(name, age, main)

profile(name = "유재석", age = 20, main = "파이썬")
profile(main = "자바", age = 25, name = "김태호")

# 함수에 순서를 설정하고, 매개 변수, 즉 키워드 값을 순서에 상관없이 불러와도 함수에 의해 함수의 순서대로 값이 불러와진다.
